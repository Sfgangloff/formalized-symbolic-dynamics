# Generated questions — Odd-shift soficity

This folder is reserved for **auxiliary questions** about the main
open problem ([`../README.md`](../README.md): is the `d`-dimensional
odd shift `Y_d` sofic?).

Empty for now. Future entries might include:

- formal sub-renderings as Lean `Prop`s (e.g. soficity / non-soficity
  separately for fixed `d`),
- the companion even-shift question `X_d` (Hochman remarks it is sofic;
  this could be formalised as a separate sub-question),
- computational experiments enumerating valid local patterns of `Y_d`
  on small windows.
